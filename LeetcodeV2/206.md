------

## 📝 算法题解：反转单链表 (Reverse Linked List)

### 1. 题目核心

反转链表的本质是**改变节点之间的指向关系**。原本是 
$$
$A \rightarrow B \rightarrow C$
$$
，我们要把它变成 
$$
$A \leftarrow B \leftarrow C$
$$
。

### 2. 逻辑拆解：三指针迭代法

为了在反转时不丢失节点，我们需要三个辅助指针：

- `prev`：指向当前节点的前驱（初始化为 `null`）。
- `curr`：指向当前正在处理的节点（初始化为 `head`）。
- `nextTemp`：临时存储 `curr` 的后继节点（防止断链）。

**每一步的动作（循环内）：**

1. **备份**：`nextTemp = curr.next` （先把“后路”记下来）。
2. **反转**：`curr.next = prev` （把当前节点的箭头指向前一个）。
3. **前移**：`prev = curr`，`curr = nextTemp` （两个指针整体向后挪一位）。

### 3. 代码实现

#### Python 版

Python

```
def reverseList(head: ListNode) -> ListNode:
    prev = None
    curr = head
    while curr:
        next_temp = curr.next  # 1. 存后路
        curr.next = prev       # 2. 调转方向
        prev = curr            # 3. prev 前移
        curr = next_temp       # 4. curr 前移
    return prev
```

#### Java 版

Java

```
public ListNode reverseList(ListNode head) {
    ListNode prev = null;
    ListNode curr = head;
    while (curr != null) {
        ListNode nextTemp = curr.next; // 1. 备份
        curr.next = prev;              // 2. 反向
        prev = curr;                   // 3. 挪 prev
        curr = nextTemp;               // 4. 挪 curr
    }
    return prev;
}
```

